A pip-installable package that uses MCMC methods to simulate the ising model, and compares results to off-the-shelf options (i.e. emcee).

Follow install directions in ../README.txt

Run ising-run as a CLI command in terminal. See main.py for defaults, and modify in-file as desired (if desired).
