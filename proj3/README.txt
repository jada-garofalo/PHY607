To install:

1. Clone this repository. Ensure your current working directory is proj3/
2. pip install --upgrade build setuptools
3. python -m build
4. pip install .

You should now be able to run ising-run as a CLI command in terminal. See ./isingPackage/README.txt for further instructions.
